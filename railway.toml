[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --chdir app app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --preload"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
